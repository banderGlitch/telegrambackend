# Asteroid Dodger — operator console (Vite + React)
#
# Point to your FastAPI backend (local or Railway). No secrets here: login uses
# the dashboard password minted by the API (`ADMIN_DASHBOARD_PASSWORD`).

VITE_API_BASE_URL=http://127.0.0.1:8000
